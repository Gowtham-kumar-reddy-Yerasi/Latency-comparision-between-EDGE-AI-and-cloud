# Latency-comparision-between-EDGE-AI-and-cloud
In this latency check would be performed using MNIST data set and further the analysis metrics would be available to compare between the EDGE AI and the cloud (which will be mimiced as a local one but not exact cloud interaction)
